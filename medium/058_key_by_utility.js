/**
 * Problem 058: Key By Utility
 * Difficulty: Medium
 * Domain: JS Utilities
 * Companies: Lodash, Google
 * 
 * Problem Description:
 * Create `keyBy(arr, iteratee)` creating object composed of keys generated from `iteratee`.
 * 
 * Example 1:
 * Input: keyBy([{ id: 'a', val: 1 }, { id: 'b', val: 2 }], 'id')
 * Output: { a: { id: 'a', val: 1 }, b: { id: 'b', val: 2 } }
 * 
 * Key Requirements:
 * - Last element overwrites key if duplicate
 */

function keyBy(...args) {
  // Write your solution here
}

// Test Cases
// console.log(keyBy(/* test args */));

module.exports = { keyBy };
