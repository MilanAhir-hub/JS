/**
 * Problem 091: URL Params Serializer
 * Difficulty: Easy
 * Domain: Web APIs
 * Companies: Axios, Meta
 * 
 * Problem Description:
 * Create `serializeParams(paramsObj)` that converts object to URL parameter string.
 * 
 * Example 1:
 * Input: serializeParams({ a: 1, b: [2, 3] })
 * Output: 'a=1&b[]=2&b[]=3'
 * 
 * Key Requirements:
 * - Handle arrays in param objects
 */

function serializeParams(...args) {
  // Write your solution here
}

// Test Cases
// console.log(serializeParams(/* test args */));

module.exports = { serializeParams };
