/**
 * Problem 031: Check Any Positive Number
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (some())
 * 
 * Problem Description:
 * Write a function `hasPositive(numbers)` that returns `true` if at least one number is > 0 using `some()`.
 * 
 * Example:
 * Input: hasPositive([-5, -2, 0, 3])
 * Output: true
 * 
 * Key Requirements:
 * - Must use `some()`
 * - Return boolean
 */

function hasPositive(numbers) {
  // Write your solution here
}

// Test Cases
console.log(hasPositive([-5, -2, 0, 3])); // Expected output: true

module.exports = { hasPositive };
