/**
 * Problem 080: Get Top K Largest Numbers
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (sort(), slice())
 * 
 * Problem Description:
 * Write a function `getTopK(numbers, k)` that returns the top `k` largest numbers in descending order using `slice()` and `sort()`.
 * 
 * Example:
 * Input: getTopK([10, 5, 20, 8, 15], 3)
 * Output: [20, 15, 10]
 * 
 * Key Requirements:
 * - Copy with `slice()`
 * - Sort descending with `sort()`
 * - Take first K with `slice(0, k)`
 */

function getTopK(numbers, k) {
  // Write your solution here
}

// Test Cases
console.log(getTopK([10, 5, 20, 8, 15], 3)); // Expected output: [20, 15, 10]

module.exports = { getTopK };
