/**
 * Problem 054: Deep Merge Objects Utility
 * Difficulty: Medium
 * Domain: JS Utilities
 * Companies: Lodash, Meta
 * 
 * Problem Description:
 * Implement `deepMerge(target, source)` recursively merging properties of source into target object.
 * 
 * Example 1:
 * Input: deepMerge({ a: { x: 1 } }, { a: { y: 2 } })
 * Output: { a: { x: 1, y: 2 } }
 * 
 * Key Requirements:
 * - Recursively merge nested objects instead of overwriting
 */

function deepMerge(...args) {
  // Write your solution here
}

// Test Cases
// console.log(deepMerge(/* test args */));

module.exports = { deepMerge };
