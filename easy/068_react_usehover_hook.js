/**
 * Problem 068: React useHover Hook
 * Difficulty: Easy
 * Domain: React Hooks
 * Companies: Meta, Airbnb
 * 
 * Problem Description:
 * Implement custom React hook `useHover()` that returns a ref and boolean state indicating if element is hovered.
 * 
 * Example 1:
 * Input: const [ref, isHovered] = useHover()
 * Output: isHovered is true when mouse enters element
 * 
 * Key Requirements:
 * - Attach mouseenter and mouseleave event listeners
 */

function useHover(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useHover(/* test args */));

module.exports = { useHover };
