/**
 * Problem 011: Rotate Array Left by K
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (shift(), push())
 * 
 * Problem Description:
 * Write a function `rotateLeft(arr, k)` that rotates an array to the left by `k` steps using `shift()` and `push()`.
 * 
 * Example:
 * Input: rotateLeft([1, 2, 3, 4, 5], 2)
 * Output: [3, 4, 5, 1, 2]
 * 
 * Key Requirements:
 * - Must use `shift()` and `push()`
 * - Mutate array in-place
 */

function rotateLeft(arr, k) {
  // Write your solution here
}

// Test Cases
console.log(rotateLeft([1, 2, 3, 4, 5], 2)); // Expected output: [3, 4, 5, 1, 2]

module.exports = { rotateLeft };
