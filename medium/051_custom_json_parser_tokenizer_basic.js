/**
 * Problem 051: Custom JSON Parser Tokenizer (Basic)
 * Difficulty: Medium
 * Domain: Parsers / Compilers
 * Companies: Meta, Google
 * 
 * Problem Description:
 * Implement `jsonParse(jsonStr)` parsing basic JSON strings into JS objects/arrays/primitives without using eval/JSON.parse.
 * 
 * Example 1:
 * Input: jsonParse('{"a": 123}')
 * Output: { a: 123 }
 * 
 * Key Requirements:
 * - Build recursive descent parser / tokenizer
 */

function jsonParse(...args) {
  // Write your solution here
}

// Test Cases
// console.log(jsonParse(/* test args */));

module.exports = { jsonParse };
