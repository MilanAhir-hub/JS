/**
 * Problem 080: React useOrientation Hook
 * Difficulty: Medium
 * Domain: React Hooks
 * Companies: Meta, Uber
 * 
 * Problem Description:
 * Implement custom React hook `useOrientation()` tracking screen orientation (`portrait` / `landscape`).
 * 
 * Example 1:
 * Input: const { angle, type } = useOrientation()
 * Output: Updates on window.screen.orientation change
 * 
 * Key Requirements:
 * - Listen to screen.orientation change event
 */

function useOrientation(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useOrientation(/* test args */));

module.exports = { useOrientation };
