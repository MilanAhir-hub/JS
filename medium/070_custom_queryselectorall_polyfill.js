/**
 * Problem 070: Custom querySelectorAll Polyfill
 * Difficulty: Medium
 * Domain: DOM APIs
 * Companies: Meta, Browser
 * 
 * Problem Description:
 * Implement `myQuerySelectorAll(root, selector)` matching simple CSS selectors (`.class`, `#id`, `tag`).
 * 
 * Example 1:
 * Input: myQuerySelectorAll(document.body, '.btn')
 * Output: Array of matching DOM elements
 * 
 * Key Requirements:
 * - Parse basic class, ID, and tag name selectors
 */

function myQuerySelectorAll(...args) {
  // Write your solution here
}

// Test Cases
// console.log(myQuerySelectorAll(/* test args */));

module.exports = { myQuerySelectorAll };
