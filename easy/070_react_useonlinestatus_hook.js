/**
 * Problem 070: React useOnlineStatus Hook
 * Difficulty: Easy
 * Domain: React Hooks
 * Companies: Meta, Uber
 * 
 * Problem Description:
 * Implement custom React hook `useOnlineStatus()` that tracks network online/offline state.
 * 
 * Example 1:
 * Input: const isOnline = useOnlineStatus()
 * Output: Returns true if navigator.onLine
 * 
 * Key Requirements:
 * - Listen to 'online' and 'offline' window events
 */

function useOnlineStatus(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useOnlineStatus(/* test args */));

module.exports = { useOnlineStatus };
