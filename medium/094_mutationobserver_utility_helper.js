/**
 * Problem 094: MutationObserver Utility Helper
 * Difficulty: Medium
 * Domain: Web APIs
 * Companies: Meta, Google
 * 
 * Problem Description:
 * Create `observeDOM(element, options, callback)` helper tracking DOM mutations.
 * 
 * Example 1:
 * Input: observeDOM(document.body, { childList: true }, mutations => ...)
 * Output: Triggers callback on DOM element changes
 * 
 * Key Requirements:
 * - Wrap native MutationObserver API
 */

function observeDOM(...args) {
  // Write your solution here
}

// Test Cases
// console.log(observeDOM(/* test args */));

module.exports = { observeDOM };
