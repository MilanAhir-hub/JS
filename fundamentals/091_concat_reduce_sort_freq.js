/**
 * Problem 091: Merged Word Frequency List
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (concat(), reduce(), sort())
 * 
 * Problem Description:
 * Write a function `getSortedWordFrequencies(list1, list2)` that merges lists with `concat()`, counts frequencies with `reduce()`, and returns `[[word, count]]` sorted by count descending.
 * 
 * Example:
 * Input: getSortedWordFrequencies(["a", "b"], ["a", "c"])
 * Output: [["a", 2], ["b", 1], ["c", 1]]
 * 
 * Key Requirements:
 * - Use `concat()`, `reduce()`, `Object.entries()`, `sort()`
 */

function getSortedWordFrequencies(list1, list2) {
  // Write your solution here
}

// Test Cases
console.log(getSortedWordFrequencies(["a", "b"], ["a", "c"]));

module.exports = { getSortedWordFrequencies };
