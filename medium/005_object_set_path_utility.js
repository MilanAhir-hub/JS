/**
 * Problem 005: Object Set Path Utility
 * Difficulty: Medium
 * Domain: Advanced JS
 * Companies: Lodash, Redux, Meta
 * 
 * Problem Description:
 * Implement `set(obj, path, value)` that sets a value at nested object path, creating objects/arrays as needed.
 * 
 * Example 1:
 * Input: set({}, 'a[0].b', 2)
 * Output: { a: [{ b: 2 }] }
 * 
 * Key Requirements:
 * - Support array index syntax 'a[0].b'
 * - Do not mutate un-targeted properties
 */

function set(...args) {
  // Write your solution here
}

// Test Cases
// console.log(set(/* test args */));

module.exports = { set };
