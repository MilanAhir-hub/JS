/**
 * Problem 055: Object Omit By Predicate
 * Difficulty: Medium
 * Domain: JS Utilities
 * Companies: Lodash, Google
 * 
 * Problem Description:
 * Create `omitBy(obj, predicate)` omitting properties where `predicate(val, key)` returns true.
 * 
 * Example 1:
 * Input: omitBy({ a: 1, b: 'hi', c: 2 }, val => typeof val === 'string')
 * Output: { a: 1, c: 2 }
 * 
 * Key Requirements:
 * - Filter object entries based on predicate
 */

function omitBy(...args) {
  // Write your solution here
}

// Test Cases
// console.log(omitBy(/* test args */));

module.exports = { omitBy };
