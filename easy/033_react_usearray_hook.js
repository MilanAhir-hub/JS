/**
 * Problem 033: React useArray Hook
 * Difficulty: Easy
 * Domain: React Hooks
 * Companies: Meta, Microsoft
 * 
 * Problem Description:
 * Implement custom React hook `useArray(initialArray)` returning array state and helper methods (`push`, `removeIndex`, `clear`).
 * 
 * Example 1:
 * Input: const { array, push, removeIndex } = useArray([1, 2]); push(3)
 * Output: Updates array state to [1, 2, 3]
 * 
 * Key Requirements:
 * - Provide immutable state helper functions
 */

function useArray(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useArray(/* test args */));

module.exports = { useArray };
