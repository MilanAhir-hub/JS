/**
 * Problem 088: HTML Escape Utility
 * Difficulty: Easy
 * Domain: Web Security
 * Companies: Google, OWASP
 * 
 * Problem Description:
 * Create `htmlEscape(str)` that escapes `&`, `<`, `>`, `"`, and `'` characters.
 * 
 * Example 1:
 * Input: htmlEscape('<script>')
 * Output: '&lt;script&gt;'
 * 
 * Key Requirements:
 * - Prevent basic XSS injections
 */

function htmlEscape(...args) {
  // Write your solution here
}

// Test Cases
// console.log(htmlEscape(/* test args */));

module.exports = { htmlEscape };
