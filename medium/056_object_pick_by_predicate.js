/**
 * Problem 056: Object Pick By Predicate
 * Difficulty: Medium
 * Domain: JS Utilities
 * Companies: Lodash, Amazon
 * 
 * Problem Description:
 * Create `pickBy(obj, predicate)` picking properties where `predicate(val, key)` returns true.
 * 
 * Example 1:
 * Input: pickBy({ a: 1, b: 'hi', c: 2 }, val => typeof val === 'number')
 * Output: { a: 1, c: 2 }
 * 
 * Key Requirements:
 * - Filter object entries based on predicate
 */

function pickBy(...args) {
  // Write your solution here
}

// Test Cases
// console.log(pickBy(/* test args */));

module.exports = { pickBy };
