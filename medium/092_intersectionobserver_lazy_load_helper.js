/**
 * Problem 092: IntersectionObserver Lazy Load Helper
 * Difficulty: Medium
 * Domain: Web Performance
 * Companies: Google, Meta
 * 
 * Problem Description:
 * Implement `lazyLoadImages(selector)` replacing `data-src` with `src` when images enter viewport.
 * 
 * Example 1:
 * Input: lazyLoadImages('img.lazy')
 * Output: Loads image src when scrolled into view
 * 
 * Key Requirements:
 * - Unobserve image element after setting src
 */

function lazyLoadImages(...args) {
  // Write your solution here
}

// Test Cases
// console.log(lazyLoadImages(/* test args */));

module.exports = { lazyLoadImages };
