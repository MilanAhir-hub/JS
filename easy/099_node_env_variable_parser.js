/**
 * Problem 099: Node ENV Variable Parser
 * Difficulty: Easy
 * Domain: Node.js Config
 * Companies: Node.js Core
 * 
 * Problem Description:
 * Create `parseEnvFile(envContent)` that parses `.env` file string into key-value object.
 * 
 * Example 1:
 * Input: parseEnvFile('PORT=3000\nDB_HOST=localhost')
 * Output: { PORT: '3000', DB_HOST: 'localhost' }
 * 
 * Key Requirements:
 * - Ignore comments starting with # and empty lines
 */

function parseEnvFile(...args) {
  // Write your solution here
}

// Test Cases
// console.log(parseEnvFile(/* test args */));

module.exports = { parseEnvFile };
