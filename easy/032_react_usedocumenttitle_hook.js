/**
 * Problem 032: React useDocumentTitle Hook
 * Difficulty: Easy
 * Domain: React Hooks
 * Companies: Vercel, Shopify
 * 
 * Problem Description:
 * Implement custom React hook `useDocumentTitle(title)` that updates `document.title`.
 * 
 * Example 1:
 * Input: useDocumentTitle('Dashboard')
 * Output: Sets document.title to 'Dashboard'
 * 
 * Key Requirements:
 * - Restore original title on unmount if requested
 */

function useDocumentTitle(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useDocumentTitle(/* test args */));

module.exports = { useDocumentTitle };
