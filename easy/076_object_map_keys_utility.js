/**
 * Problem 076: Object Map Keys Utility
 * Difficulty: Easy
 * Domain: JS Utilities
 * Companies: Lodash, Meta
 * 
 * Problem Description:
 * Create `mapKeys(obj, iteratee)` that transforms object keys using an iteratee function.
 * 
 * Example 1:
 * Input: mapKeys({ a: 1, b: 2 }, (val, key) => key + val)
 * Output: { a1: 1, b2: 2 }
 * 
 * Key Requirements:
 * - Preserve original values
 */

function mapKeys(...args) {
  // Write your solution here
}

// Test Cases
// console.log(mapKeys(/* test args */));

module.exports = { mapKeys };
