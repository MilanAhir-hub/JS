/**
 * Problem 071: Map Items to Priority Rank Index
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (map(), indexOf())
 * 
 * Problem Description:
 * Write a function `getPriorityRanks(items, priorityOrder)` that maps each item to its index position in `priorityOrder` using `map()` and `indexOf()`.
 * 
 * Example:
 * Input: getPriorityRanks(["med", "high"], ["low", "med", "high"])
 * Output: [1, 2]
 * 
 * Key Requirements:
 * - Must use `map()` and `indexOf()`
 */

function getPriorityRanks(items, priorityOrder) {
  // Write your solution here
}

// Test Cases
console.log(getPriorityRanks(["med", "high"], ["low", "med", "high"])); // Expected output: [1, 2]

module.exports = { getPriorityRanks };
