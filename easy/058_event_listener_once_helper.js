/**
 * Problem 058: Event Listener Once Helper
 * Difficulty: Easy
 * Domain: DOM / Node.js
 * Companies: Meta, Google
 * 
 * Problem Description:
 * Create `onceListener(target, event, handler)` that attaches an event listener that automatically detaches after triggering once.
 * 
 * Example 1:
 * Input: onceListener(button, 'click', handleClick)
 * Output: Triggers handleClick once on click then removes listener
 * 
 * Key Requirements:
 * - Use addEventListener and removeEventListener
 */

function onceListener(...args) {
  // Write your solution here
}

// Test Cases
// console.log(onceListener(/* test args */));

module.exports = { onceListener };
