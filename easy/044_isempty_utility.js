/**
 * Problem 044: isEmpty Utility
 * Difficulty: Easy
 * Domain: JS Utilities
 * Companies: Lodash, Amazon
 * 
 * Problem Description:
 * Create `isEmpty(value)` that checks if value is an empty object, collection, map, or set.
 * 
 * Example 1:
 * Input: isEmpty({})
 * Output: true
 * 
 * Key Requirements:
 * - Handle null, undefined, arrays, objects, maps, sets
 */

function isEmpty(...args) {
  // Write your solution here
}

// Test Cases
// console.log(isEmpty(/* test args */));

module.exports = { isEmpty };
