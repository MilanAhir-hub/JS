/**
 * Problem 079: React useElementSize Hook
 * Difficulty: Medium
 * Domain: React Hooks
 * Companies: Meta, Vercel
 * 
 * Problem Description:
 * Implement custom React hook `useElementSize()` returning `[ref, { width, height }]` using ResizeObserver.
 * 
 * Example 1:
 * Input: const [ref, { width, height }] = useElementSize()
 * Output: Tracks element width and height on resize
 * 
 * Key Requirements:
 * - Use ResizeObserver API to track dimensions
 */

function useElementSize(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useElementSize(/* test args */));

module.exports = { useElementSize };
