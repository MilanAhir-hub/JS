/**
 * Problem 072: Find and Remove User by ID
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (find(), indexOf(), splice())
 * 
 * Problem Description:
 * Write a function `removeUserById(users, id)` that finds the user object by `id` and removes it from `users` array in-place.
 * 
 * Example:
 * Input: removeUserById([{id: 1}, {id: 2}], 1)
 * Output: [{id: 2}]
 * 
 * Key Requirements:
 * - Find target with `find()` or `findIndex()`
 * - Remove with `splice()`
 */

function removeUserById(users, id) {
  // Write your solution here
}

// Test Cases
console.log(removeUserById([{id: 1}, {id: 2}], 1)); // Expected output: [{id: 2}]

module.exports = { removeUserById };
