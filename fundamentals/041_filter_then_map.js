/**
 * Problem 041: Filter Positives and Square
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (filter(), map())
 * 
 * Problem Description:
 * Write a function `squarePositives(numbers)` that filters out non-positive numbers and squares remaining using `filter()` then `map()`.
 * 
 * Example:
 * Input: squarePositives([-2, 0, 3, 4])
 * Output: [9, 16]
 * 
 * Key Requirements:
 * - Chain `filter()` and `map()`
 */

function squarePositives(numbers) {
  // Write your solution here
}

// Test Cases
console.log(squarePositives([-2, 0, 3, 4])); // Expected output: [9, 16]

module.exports = { squarePositives };
