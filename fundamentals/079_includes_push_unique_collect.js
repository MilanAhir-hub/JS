/**
 * Problem 079: Collect Unique Items Only
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (includes(), push())
 * 
 * Problem Description:
 * Write a function `collectUnique(arr)` that loops through `arr` and pushes items to output array only if `includes()` returns false.
 * 
 * Example:
 * Input: collectUnique(["a", "b", "a", "c"])
 * Output: ["a", "b", "c"]
 * 
 * Key Requirements:
 * - Use `includes()` to check existence
 * - Use `push()` to add new element
 */

function collectUnique(arr) {
  // Write your solution here
}

// Test Cases
console.log(collectUnique(["a", "b", "a", "c"])); // Expected output: ["a", "b", "c"]

module.exports = { collectUnique };
