/**
 * Problem 069: React useWindowSize Hook
 * Difficulty: Easy
 * Domain: React Hooks
 * Companies: Meta, Vercel
 * 
 * Problem Description:
 * Implement custom React hook `useWindowSize()` returning `{ width, height }` of browser window.
 * 
 * Example 1:
 * Input: const { width, height } = useWindowSize()
 * Output: Updates on window resize event
 * 
 * Key Requirements:
 * - Debounce/throttle resize listener for performance
 */

function useWindowSize(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useWindowSize(/* test args */));

module.exports = { useWindowSize };
