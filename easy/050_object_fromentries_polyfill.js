/**
 * Problem 050: Object.fromEntries Polyfill
 * Difficulty: Easy
 * Domain: JS Core
 * Companies: ECMAScript, Meta
 * 
 * Problem Description:
 * Implement `myFromEntries(iterable)` that transforms a list of key-value pairs into an object.
 * 
 * Example 1:
 * Input: myFromEntries([['a', 1], ['b', 2]])
 * Output: { a: 1, b: 2 }
 * 
 * Key Requirements:
 * - Accept array of pairs or Map
 */

function myFromEntries(...args) {
  // Write your solution here
}

// Test Cases
// console.log(myFromEntries(/* test args */));

module.exports = { myFromEntries };
