/**
 * Problem 058: Sort Numbers Descending
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (sort())
 * 
 * Problem Description:
 * Write a function `sortDescending(numbers)` that sorts numeric array in descending order using `sort()`.
 * 
 * Example:
 * Input: sortDescending([10, 2, 5, 1, 8])
 * Output: [10, 8, 5, 2, 1]
 * 
 * Key Requirements:
 * - Must use comparator `(a, b) => b - a`
 */

function sortDescending(numbers) {
  // Write your solution here
}

// Test Cases
console.log(sortDescending([10, 2, 5, 1, 8])); // Expected output: [10, 8, 5, 2, 1]

module.exports = { sortDescending };
