/**
 * Problem 083: Array Difference (Items in A not B)
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (filter(), includes())
 * 
 * Problem Description:
 * Write a function `arrayDifference(arrA, arrB)` that returns elements in `arrA` that are not present in `arrB` using `filter()` and `includes()`.
 * 
 * Example:
 * Input: arrayDifference([1, 2, 3, 4], [2, 4])
 * Output: [1, 3]
 * 
 * Key Requirements:
 * - Use `filter()` on `arrA`
 * - Check `!arrB.includes(item)`
 */

function arrayDifference(arrA, arrB) {
  // Write your solution here
}

// Test Cases
console.log(arrayDifference([1, 2, 3, 4], [2, 4])); // Expected output: [1, 3]

module.exports = { arrayDifference };
