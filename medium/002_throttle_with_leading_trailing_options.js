/**
 * Problem 002: Throttle with Leading & Trailing Options
 * Difficulty: Medium
 * Domain: Advanced JS
 * Companies: Meta, Google, Netflix
 * 
 * Problem Description:
 * Implement an advanced `throttle` utility supporting `{ leading: boolean, trailing: boolean }` options and `.cancel()` method.
 * 
 * Example 1:
 * Input: throttle(fn, 200, { leading: false, trailing: true })
 * Output: Throttles calls with configurable edges
 * 
 * Key Requirements:
 * - Support leading & trailing execution config
 * - Provide .cancel() method
 */

function throttle(...args) {
  // Write your solution here
}

// Test Cases
// console.log(throttle(/* test args */));

module.exports = { throttle };
