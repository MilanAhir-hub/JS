/**
 * Problem 021: Merge Two Arrays
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (concat())
 * 
 * Problem Description:
 * Write a function `mergeArrays(arr1, arr2)` that returns a combined array using `concat()`.
 * 
 * Example:
 * Input: mergeArrays([1, 2], [3, 4])
 * Output: [1, 2, 3, 4]
 * 
 * Key Requirements:
 * - Must use `concat()`
 * - Neither input array modified
 */

function mergeArrays(arr1, arr2) {
  // Write your solution here
}

// Test Cases
console.log(mergeArrays([1, 2], [3, 4])); // Expected output: [1, 2, 3, 4]

module.exports = { mergeArrays };
