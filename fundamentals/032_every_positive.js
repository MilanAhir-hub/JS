/**
 * Problem 032: Check All Positive Numbers
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (every())
 * 
 * Problem Description:
 * Write a function `allPositive(numbers)` that returns `true` if all numbers are > 0 using `every()`.
 * 
 * Example:
 * Input: allPositive([1, 2, 3, 4])
 * Output: true
 * 
 * Key Requirements:
 * - Must use `every()`
 * - Return boolean
 */

function allPositive(numbers) {
  // Write your solution here
}

// Test Cases
console.log(allPositive([1, 2, 3, 4])); // Expected output: true

module.exports = { allPositive };
