/**
 * Problem 043: Invert Object Keys and Values
 * Difficulty: Easy
 * Domain: JS Utilities
 * Companies: Lodash, Google
 * 
 * Problem Description:
 * Create `invert(obj)` that creates an object composed of the inverted keys and values.
 * 
 * Example 1:
 * Input: invert({ a: 'x', b: 'y' })
 * Output: { x: 'a', y: 'b' }
 * 
 * Key Requirements:
 * - Convert values to strings as keys
 */

function invert(...args) {
  // Write your solution here
}

// Test Cases
// console.log(invert(/* test args */));

module.exports = { invert };
