/**
 * Problem 066: React useInterval Hook
 * Difficulty: Easy
 * Domain: React Hooks
 * Companies: Meta, Vercel
 * 
 * Problem Description:
 * Implement custom React hook `useInterval(callback, delay)` that sets up an interval declaratively.
 * 
 * Example 1:
 * Input: useInterval(() => setTicks(t => t + 1), 1000)
 * Output: Increments ticks every second
 * 
 * Key Requirements:
 * - Pause interval if delay is null
 */

function useInterval(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useInterval(/* test args */));

module.exports = { useInterval };
