/**
 * Problem 049: Object.entries Polyfill
 * Difficulty: Easy
 * Domain: JS Core
 * Companies: ECMAScript, Google
 * 
 * Problem Description:
 * Implement `myEntries(obj)` that returns an array of a given object's own enumerable string-keyed property [key, value] pairs.
 * 
 * Example 1:
 * Input: myEntries({ a: 1, b: 2 })
 * Output: [['a', 1], ['b', 2]]
 * 
 * Key Requirements:
 * - Iterate own enumerable properties only
 */

function myEntries(...args) {
  // Write your solution here
}

// Test Cases
// console.log(myEntries(/* test args */));

module.exports = { myEntries };
