/**
 * Problem 040: Clamp Number Utility
 * Difficulty: Easy
 * Domain: JS Utilities
 * Companies: Lodash, Meta
 * 
 * Problem Description:
 * Create `clamp(num, lower, upper)` that clamps `num` within the inclusive `lower` and `upper` bounds.
 * 
 * Example 1:
 * Input: clamp(-10, -5, 5)
 * Output: -5
 * 
 * Key Requirements:
 * - Return bounds if num exceeds range
 */

function clamp(...args) {
  // Write your solution here
}

// Test Cases
// console.log(clamp(/* test args */));

module.exports = { clamp };
