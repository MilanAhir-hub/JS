/**
 * Problem 013: Get Subarray using Slice
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (slice())
 * 
 * Problem Description:
 * Write a function `getSubarray(arr, start, end)` that extracts a portion of `arr` from index `start` to `end` (exclusive) using `slice()`.
 * 
 * Example:
 * Input: getSubarray([10, 20, 30, 40, 50], 1, 4)
 * Output: [20, 30, 40]
 * 
 * Key Requirements:
 * - Must use `slice()`
 * - Original array must remain unchanged
 */

function getSubarray(arr, start, end) {
  // Write your solution here
}

// Test Cases
console.log(getSubarray([10, 20, 30, 40, 50], 1, 4)); // Expected output: [20, 30, 40]

module.exports = { getSubarray };
