/**
 * Problem 093: Sliding Window Sum Calculator
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (shift(), push(), reduce())
 * 
 * Problem Description:
 * Write a function `slidingWindowSums(nums, k)` that computes the sum of every sliding window of size `k` using queue operations (`shift()`, `push()`, `reduce()`).
 * 
 * Example:
 * Input: slidingWindowSums([1, 2, 3, 4, 5], 3)
 * Output: [6, 9, 12]
 * 
 * Key Requirements:
 * - Maintain window of size K
 * - Return array of window sums
 */

function slidingWindowSums(nums, k) {
  // Write your solution here
}

// Test Cases
console.log(slidingWindowSums([1, 2, 3, 4, 5], 3)); // Expected output: [6, 9, 12]

module.exports = { slidingWindowSums };
