/**
 * Problem 075: Move Item to Front by Value
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (indexOf(), splice(), unshift())
 * 
 * Problem Description:
 * Write a function `moveToFront(arr, item)` that finds `item` index, removes it with `splice()`, and prepends it with `unshift()`.
 * 
 * Example:
 * Input: moveToFront(["a", "b", "c"], "c")
 * Output: ["c", "a", "b"]
 * 
 * Key Requirements:
 * - Find index with `indexOf()`
 * - Use `splice()` and `unshift()`
 */

function moveToFront(arr, item) {
  // Write your solution here
}

// Test Cases
console.log(moveToFront(["a", "b", "c"], "c")); // Expected output: ["c", "a", "b"]

module.exports = { moveToFront };
