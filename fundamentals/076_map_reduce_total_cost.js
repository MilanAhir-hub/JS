/**
 * Problem 076: Calculate Total Taxed Cart Price
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (map(), reduce())
 * 
 * Problem Description:
 * Write a function `calculateTotalCost(items, taxRate)` that calculates price with tax for each item and returns total sum using `map()` and `reduce()`.
 * 
 * Example:
 * Input: calculateTotalCost([{price: 100}, {price: 200}], 0.1)
 * Output: 330
 * 
 * Key Requirements:
 * - Use `map()` to add tax
 * - Use `reduce()` to sum total
 */

function calculateTotalCost(items, taxRate) {
  // Write your solution here
}

// Test Cases
console.log(calculateTotalCost([{price: 100}, {price: 200}], 0.1)); // Expected output: 330

module.exports = { calculateTotalCost };
