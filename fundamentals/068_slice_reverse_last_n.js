/**
 * Problem 068: Reverse Last N Elements
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (slice(), reverse())
 * 
 * Problem Description:
 * Write a function `reverseLastN(arr, n)` that extracts last N elements using `slice()` and reverses them using `reverse()`.
 * 
 * Example:
 * Input: reverseLastN([1, 2, 3, 4, 5], 3)
 * Output: [5, 4, 3]
 * 
 * Key Requirements:
 * - Use `slice(-n)` and `reverse()`
 */

function reverseLastN(arr, n) {
  // Write your solution here
}

// Test Cases
console.log(reverseLastN([1, 2, 3, 4, 5], 3)); // Expected output: [5, 4, 3]

module.exports = { reverseLastN };
