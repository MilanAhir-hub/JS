/**
 * Problem 067: React useCounter Hook
 * Difficulty: Easy
 * Domain: React Hooks
 * Companies: Meta, Stripe
 * 
 * Problem Description:
 * Implement custom React hook `useCounter(initialValue)` returning `count`, `increment`, `decrement`, `reset`.
 * 
 * Example 1:
 * Input: const { count, increment, reset } = useCounter(0); increment();
 * Output: Updates count to 1
 * 
 * Key Requirements:
 * - Provide clean updater functions
 */

function useCounter(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useCounter(/* test args */));

module.exports = { useCounter };
