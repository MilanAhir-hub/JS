/**
 * Problem 078: Chunk Array using Reduce and Push
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (reduce(), push())
 * 
 * Problem Description:
 * Write a function `reduceChunk(arr, size)` that splits `arr` into chunks of size `size` using `reduce()` and `push()`.
 * 
 * Example:
 * Input: reduceChunk([1, 2, 3, 4], 2)
 * Output: [[1, 2], [3, 4]]
 * 
 * Key Requirements:
 * - Must use `reduce()` accumulator as array of chunks
 * - Use `push()` inside reduce accumulator
 */

function reduceChunk(arr, size) {
  // Write your solution here
}

// Test Cases
console.log(reduceChunk([1, 2, 3, 4], 2)); // Expected output: [[1, 2], [3, 4]]

module.exports = { reduceChunk };
