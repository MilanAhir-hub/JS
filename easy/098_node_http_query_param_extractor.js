/**
 * Problem 098: Node HTTP Query Param Extractor
 * Difficulty: Easy
 * Domain: Node.js HTTP
 * Companies: Node.js Core
 * 
 * Problem Description:
 * Create helper `extractQueryParams(reqUrl)` that parses query parameters from raw `req.url` in vanilla HTTP server.
 * 
 * Example 1:
 * Input: extractQueryParams('/api/users?page=1&limit=10')
 * Output: { page: '1', limit: '10' }
 * 
 * Key Requirements:
 * - Use URL class or manual string splitting
 */

function extractQueryParams(...args) {
  // Write your solution here
}

// Test Cases
// console.log(extractQueryParams(/* test args */));

module.exports = { extractQueryParams };
