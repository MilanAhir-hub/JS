/**
 * Problem 052: Array.prototype.flatMap Polyfill
 * Difficulty: Easy
 * Domain: JS Core
 * Companies: Google, Apple
 * 
 * Problem Description:
 * Implement `myFlatMap(fn)` on Array prototype that maps each element using a mapping function, then flattens the result by 1 level.
 * 
 * Example 1:
 * Input: [1, 2].myFlatMap(x => [x, x * 2])
 * Output: [1, 2, 2, 4]
 * 
 * Key Requirements:
 * - Equivalent to map followed by flat(1)
 */

function myFlatMap(...args) {
  // Write your solution here
}

// Test Cases
// console.log(myFlatMap(/* test args */));

module.exports = { myFlatMap };
