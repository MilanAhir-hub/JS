/**
 * Problem 072: Node Simple JSON File Storage
 * Difficulty: Easy
 * Domain: Node.js FS
 * Companies: Node.js Core
 * 
 * Problem Description:
 * Create `JsonStorage(filePath)` class with `get(key)`, `set(key, val)`, and `save()` methods.
 * 
 * Example 1:
 * Input: const db = new JsonStorage('db.json'); db.set('user', 'John'); db.save();
 * Output: Persists JSON data to disk
 * 
 * Key Requirements:
 * - Read and write file using fs.readFileSync / writeFileSync
 */

function JsonStorage(...args) {
  // Write your solution here
}

// Test Cases
// console.log(JsonStorage(/* test args */));

module.exports = { JsonStorage };
