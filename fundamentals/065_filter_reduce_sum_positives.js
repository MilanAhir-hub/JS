/**
 * Problem 065: Sum of Positive Numbers Only
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (filter(), reduce())
 * 
 * Problem Description:
 * Write a function `sumPositives(numbers)` that filters positive numbers and calculates their total sum using `filter()` and `reduce()`.
 * 
 * Example:
 * Input: sumPositives([-3, 5, 2, -1, 4])
 * Output: 11
 * 
 * Key Requirements:
 * - Chain `filter()` and `reduce()`
 */

function sumPositives(numbers) {
  // Write your solution here
}

// Test Cases
console.log(sumPositives([-3, 5, 2, -1, 4])); // Expected output: 11

module.exports = { sumPositives };
