/**
 * Problem 089: Update Matching Item In-Place
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (find(), indexOf(), splice())
 * 
 * Problem Description:
 * Write a function `updateProductPrice(products, id, newPrice)` that finds product by `id`, locates index with `indexOf()`, and replaces product with updated price using `splice()`.
 * 
 * Example:
 * Input: updateProductPrice([{id: 1, price: 10}], 1, 15)
 * Output: [{id: 1, price: 15}]
 * 
 * Key Requirements:
 * - Find item with `find()`
 * - Locate index with `indexOf()`
 * - Replace object using `splice()`
 */

function updateProductPrice(products, id, newPrice) {
  // Write your solution here
}

// Test Cases
console.log(updateProductPrice([{id: 1, price: 10}], 1, 15));

module.exports = { updateProductPrice };
