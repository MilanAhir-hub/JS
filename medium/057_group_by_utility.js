/**
 * Problem 057: Group By Utility
 * Difficulty: Medium
 * Domain: JS Utilities
 * Companies: Lodash, Meta
 * 
 * Problem Description:
 * Create `groupBy(arr, iteratee)` grouping array elements by key returned from `iteratee`.
 * 
 * Example 1:
 * Input: groupBy([6.1, 4.2, 6.3], Math.floor)
 * Output: { '4': [4.2], '6': [6.1, 6.3] }
 * 
 * Key Requirements:
 * - Support property name string or callback function iteratee
 */

function groupBy(...args) {
  // Write your solution here
}

// Test Cases
// console.log(groupBy(/* test args */));

module.exports = { groupBy };
