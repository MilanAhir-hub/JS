/**
 * Problem 084: Sanitize and Sort Valid Emails
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (map(), filter(), sort())
 * 
 * Problem Description:
 * Write a function `processEmails(emails)` that trims and lowercases emails using `map()`, filters those containing `@` with `includes()`, and sorts alphabetically using `sort()`.
 * 
 * Example:
 * Input: processEmails([" Bob@mail.com ", "invalid", "alice@mail.com"])
 * Output: ["alice@mail.com", "bob@mail.com"]
 * 
 * Key Requirements:
 * - Chain `map()`, `filter()`, and `sort()`
 */

function processEmails(emails) {
  // Write your solution here
}

// Test Cases
console.log(processEmails([" Bob@mail.com ", "invalid", "alice@mail.com"]));

module.exports = { processEmails };
