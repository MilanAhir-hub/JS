/**
 * Problem 040: Filter Active Users
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (filter())
 * 
 * Problem Description:
 * Write a function `getActiveUsers(users)` that filters objects where `active === true` using `filter()`.
 * 
 * Example:
 * Input: getActiveUsers([{id: 1, active: true}, {id: 2, active: false}])
 * Output: [{id: 1, active: true}]
 * 
 * Key Requirements:
 * - Must use `filter()`
 */

function getActiveUsers(users) {
  // Write your solution here
}

// Test Cases
console.log(getActiveUsers([{id: 1, active: true}, {id: 2, active: false}])); // Expected output: [{id: 1, active: true}]

module.exports = { getActiveUsers };
