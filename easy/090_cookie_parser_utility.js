/**
 * Problem 090: Cookie Parser Utility
 * Difficulty: Easy
 * Domain: Web APIs / Node.js
 * Companies: Express, Meta
 * 
 * Problem Description:
 * Create `parseCookies(cookieHeader)` that converts document.cookie or request cookie header into object.
 * 
 * Example 1:
 * Input: parseCookies('foo=bar; equation=E%3Dmc%5E2')
 * Output: { foo: 'bar', equation: 'E=mc^2' }
 * 
 * Key Requirements:
 * - Decode URI component cookie values
 */

function parseCookies(...args) {
  // Write your solution here
}

// Test Cases
// console.log(parseCookies(/* test args */));

module.exports = { parseCookies };
