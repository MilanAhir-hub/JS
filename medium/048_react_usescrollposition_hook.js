/**
 * Problem 048: React useScrollPosition Hook
 * Difficulty: Medium
 * Domain: React Hooks
 * Companies: Meta, Vercel
 * 
 * Problem Description:
 * Implement custom React hook `useScrollPosition()` returning `{ scrollX, scrollY }`.
 * 
 * Example 1:
 * Input: const { scrollY } = useScrollPosition()
 * Output: Updates scroll coordinates on window scroll
 * 
 * Key Requirements:
 * - Throttle scroll event listener
 */

function useScrollPosition(...args) {
  // Write your solution here
}

// Test Cases
// console.log(useScrollPosition(/* test args */));

module.exports = { useScrollPosition };
