/**
 * Problem 034: classNames Utility Function
 * Difficulty: Easy
 * Domain: React Utilities
 * Companies: Meta, Vercel
 * 
 * Problem Description:
 * Implement `classNames(...args)` that conditionally joins CSS class names together.
 * 
 * Example 1:
 * Input: classNames('foo', { bar: true, duck: false }, 'baz')
 * Output: 'foo bar baz'
 * 
 * Key Requirements:
 * - Handle strings, numbers, objects, and arrays
 */

function classNames(...args) {
  // Write your solution here
}

// Test Cases
// console.log(classNames(/* test args */));

module.exports = { classNames };
