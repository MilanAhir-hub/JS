/**
 * Problem 039: inRange Utility
 * Difficulty: Easy
 * Domain: JS Utilities
 * Companies: Lodash, Amazon
 * 
 * Problem Description:
 * Create `inRange(num, start, end)` that checks if `num` is between `start` and up to `end`.
 * 
 * Example 1:
 * Input: inRange(3, 2, 4)
 * Output: true
 * 
 * Key Requirements:
 * - Swap start and end if start > end
 */

function inRange(...args) {
  // Write your solution here
}

// Test Cases
// console.log(inRange(/* test args */));

module.exports = { inRange };
