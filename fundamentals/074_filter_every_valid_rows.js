/**
 * Problem 074: Filter Rows Where All Values Positive
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (filter(), every())
 * 
 * Problem Description:
 * Write a function `filterPositiveRows(matrix)` that filters rows where every number is > 0 using `filter()` and `every()`.
 * 
 * Example:
 * Input: filterPositiveRows([[1, 2], [-1, 3], [4, 5]])
 * Output: [[1, 2], [4, 5]]
 * 
 * Key Requirements:
 * - Use `filter()` on matrix
 * - Use `every()` on row elements
 */

function filterPositiveRows(matrix) {
  // Write your solution here
}

// Test Cases
console.log(filterPositiveRows([[1, 2], [-1, 3], [4, 5]])); // Expected output: [[1, 2], [4, 5]]

module.exports = { filterPositiveRows };
