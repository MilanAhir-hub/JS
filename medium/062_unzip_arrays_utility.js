/**
 * Problem 062: Unzip Arrays Utility
 * Difficulty: Medium
 * Domain: JS Utilities
 * Companies: Lodash, Microsoft
 * 
 * Problem Description:
 * Create `unzip(array)` reversing `zip` grouping.
 * 
 * Example 1:
 * Input: unzip([['a', 1], ['b', 2]])
 * Output: [['a', 'b'], [1, 2]]
 * 
 * Key Requirements:
 * - Regroup elements by column index
 */

function unzip(...args) {
  // Write your solution here
}

// Test Cases
// console.log(unzip(/* test args */));

module.exports = { unzip };
