/**
 * Problem 060: Partition Array Utility
 * Difficulty: Medium
 * Domain: JS Utilities
 * Companies: Lodash, Meta
 * 
 * Problem Description:
 * Create `partition(arr, predicate)` splitting array into two arrays: truthy elements and falsey elements.
 * 
 * Example 1:
 * Input: partition([1, 2, 3, 4], x => x % 2 === 0)
 * Output: [[2, 4], [1, 3]]
 * 
 * Key Requirements:
 * - Return tuple array `[truthyArr, falseyArr]`
 */

function partition(...args) {
  // Write your solution here
}

// Test Cases
// console.log(partition(/* test args */));

module.exports = { partition };
