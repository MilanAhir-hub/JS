/**
 * Problem 046: Filter Numbers in Range
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (filter())
 * 
 * Problem Description:
 * Write a function `filterRange(numbers, min, max)` that returns numbers where `min <= num <= max` using `filter()`.
 * 
 * Example:
 * Input: filterRange([5, 10, 15, 20], 10, 15)
 * Output: [10, 15]
 * 
 * Key Requirements:
 * - Must use `filter()`
 */

function filterRange(numbers, min, max) {
  // Write your solution here
}

// Test Cases
console.log(filterRange([5, 10, 15, 20], 10, 15)); // Expected output: [10, 15]

module.exports = { filterRange };
