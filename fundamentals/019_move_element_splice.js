/**
 * Problem 019: Move Element from Index A to B
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (splice())
 * 
 * Problem Description:
 * Write a function `moveElement(arr, fromIndex, toIndex)` that moves an element from `fromIndex` to `toIndex` using `splice()`.
 * 
 * Example:
 * Input: moveElement(["a", "b", "c", "d"], 0, 2)
 * Output: ["b", "c", "a", "d"]
 * 
 * Key Requirements:
 * - Must use `splice()`
 * - Mutate and return original array
 */

function moveElement(arr, fromIndex, toIndex) {
  // Write your solution here
}

// Test Cases
console.log(moveElement(["a", "b", "c", "d"], 0, 2)); // Expected output: ["b", "c", "a", "d"]

module.exports = { moveElement };
