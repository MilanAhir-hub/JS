/**
 * Problem 081: Average Grade of Passing Students
 * Difficulty: Fundamentals
 * Domain: JS Array Methods (filter(), map(), reduce())
 * 
 * Problem Description:
 * Write a function `getPassingAverage(students)` that filters students with `score >= 60`, maps their scores, and calculates average using `reduce()`.
 * 
 * Example:
 * Input: getPassingAverage([{score: 80}, {score: 40}, {score: 100}])
 * Output: 90
 * 
 * Key Requirements:
 * - Chain `filter()`, `map()`, and `reduce()`
 * - Return 0 if no student passed
 */

function getPassingAverage(students) {
  // Write your solution here
}

// Test Cases
console.log(getPassingAverage([{score: 80}, {score: 40}, {score: 100}])); // Expected output: 90

module.exports = { getPassingAverage };
